window.SIDEBAR_ITEMS = {"enum":[["Error","Errors about decoding/encoding."]],"fn":[["decode","Decodes src into [decoded_len(src.len())][decoded_len] bytes, returning the actual number of bytes written to `dst`."],["decode_string","Returns the bytes represented by the hexadecimal string `s`."],["decoded_len","Returns the length of a decoding of `x` source bytes. Specifically, it returns `x / 2`."],["dump","Returns a string that contains a hex dump of the given data. The format of the hex dump matches the output of `hexdump -C` on the command line."],["dumper","Makes a Dumper that writes a hex dump of all written data to `w`. The format of the dump matches the output of `hexdump -C` on the command line."],["encode","Encodes `src` into [encoded_len(src.len())][encoded_len] bytes of `dst`. As a convenience, it returns the number of bytes written to `dst`, but this value is always [encoded_len(src.len())][encoded_len]. `encode` implements hexadecimal encoding."],["encode_to_string","Returns the hexadecimal encoding of `src`."],["encoded_len","Returns the length of an encoding of `n` source bytes. Specifically, it returns `n * 2`."],["new_decoder","Returns an [io::Read][std::io::Read] that decodes hexadecimal characters from `r`. `new_decoder` expects that `r` contain only an even number of hexadecimal characters."],["new_encoder","Returns an [io::Write][std::io::Write] that writes lowercase hexadecimal characters to `w`."]]};